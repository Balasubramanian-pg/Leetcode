These problems require calculating a cumulative value over a sequence of rows. The `SUM() OVER (ORDER BY ...)` window function is the star here. They are often used to solve "packing" or budget-constraint problems.

