This classic pattern involves finding consecutive streaks, gaps, or overlapping periods. The core technique uses `LAG()`, `LEAD()`, or `ROW_NUMBER()` to identify where a sequence breaks and a new "island" begins.

